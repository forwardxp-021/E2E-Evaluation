# Minimal dependencies for the embedding analysis scripts
# Install with:  pip install -r requirements-analysis.txt

numpy>=1.24
umap-learn>=0.5.5
matplotlib>=3.7

# Optional but recommended (faster CSV loading, richer data inspection)
pandas>=2.0

# Optional: joblib is used internally by umap-learn for parallelism
joblib>=1.3
