# B1-IR Remediation Report v1

**Status: `B1_IR_READY_FOR_OWNER_REVIEW`**

The governing B1 state remains `B1_TSB_BENCHMARK_QUALIFICATION_INCOMPLETE_INFRASTRUCTURE`. No B1 PASS/FAIL verdict was recomputed.

The missing official metrics were a lifecycle defect, not a scientific failure. All 34 runner/recorder-complete arms retained the official nuPlan per-scenario payload; the official callback recovered all 16 metric parquet files for each arm with no simulation. This restores official collision and drivable-area evidence for the 17 trace-complete pairs. Their original mechanism evidence remains 17/17 baseline one-phase, treatment two-stage, joint mechanism and F_match, with 0/17 LOW_SPEED_ENDSTOP. These descriptive results did not influence the remediation design or any parameter.

The future executor now blocks runner entry unless the exact production native-route builder traverses the full frozen topology and the official metric finalizer is bound. B1-18's missing successor 19339 is detected before runner entry. Five pairs share that topology issue; completed historical attempts are preserved, while B1-20 remains blocked before first execution.

Recovered official-safety pairs: 17. Prospective `ARM_COMPLETE` records under the new lifecycle: 0, because no new execution occurred and historical manifests were not rewritten. B1 still lacks authorized completion of B1-18 through B1-20 and an Owner-reviewed treatment of the frozen-route incompatibility. RBR-V2 training remains downstream of completed B1 and is not authorized.

Zero-run declaration: `SIMULATION=0`, `RUNNER_RUN=0`, `TSB_ROLLOUT=0`, `HLC_ROLLOUT=0`, `RBR_TRAINING=0`, `PRIMARY_EVALUATION=0`, `V_EXECUTION=0`, `C_EXECUTION=0`, `NEW_SCIENTIFIC_OUTCOME_EXPOSURE=0`.
